export type FollowersAPI = {
  id: number;
  login: string;
  avatar_url: string;
  html_url: string;
};

export type ResponseFollowersData = FollowersAPI & {};

export type FollowersData = FollowersAPI & {};
